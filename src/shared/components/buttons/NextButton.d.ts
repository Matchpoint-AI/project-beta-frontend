import React from 'react';
declare const NextButton: ({ text, formId, disabled, onClick, children, }: {
    text?: string;
    formId?: string;
    disabled?: boolean;
    onClick?: () => void;
    children?: React.ReactNode;
}) => import("react/jsx-runtime").JSX.Element;
export default NextButton;
