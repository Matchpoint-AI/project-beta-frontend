load("@aspect_rules_js//js:defs.bzl", "js_library")

js_library(
    name = "shared_files",
    srcs = glob(
        [
            "**/*.tsx",
            "**/*.ts",
        ],
        allow_empty = True,
        exclude = ["**/*.test.tsx"],
    ),
    visibility = ["//frontend/src:__pkg__"],
)
