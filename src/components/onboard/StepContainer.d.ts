import React from 'react';
declare const StepContainer: ({
  icon,
  title,
  children,
}: {
  icon: string;
  title: string;
  children: React.ReactNode;
}) => import('react/jsx-runtime').JSX.Element;
export default StepContainer;
