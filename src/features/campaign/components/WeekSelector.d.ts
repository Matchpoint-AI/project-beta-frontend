declare const WeekSelector: ({ options, currentValues, setCurrentValues, }: {
    options: string[];
    currentValues: string[];
    setCurrentValues: (values: string[]) => void;
}) => any;
export default WeekSelector;
